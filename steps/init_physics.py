"""Randomize physics constants and show the ground truth topology.

Stub — not yet ported to the ADVEI schema. CLI surface is preserved so
that ``pfab-mock --help`` still lists the command, but invocation raises.
Tracked under the ``Complete ADVEI mock end-to-end`` task.
"""

import argparse


def run(args: argparse.Namespace) -> None:
    raise NotImplementedError(
        "init-physics: not yet ported to the ADVEI schema. "
        "See the 'Complete ADVEI mock end-to-end' task."
    )
