[project]
name = "pred-fab-mock"
version = "0.1.0-isarc2026"
requires-python = ">=3.9"
dependencies = [
    "pred-fab",
    "matplotlib>=3.9.4",
    "numpy>=1.24",
    "scikit-learn>=1.3",
]

[tool.uv.sources]
pred-fab = { path = "../pred-fab", editable = true }
